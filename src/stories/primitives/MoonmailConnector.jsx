/* eslint-disable no-unused-vars */

import { BlockContainer } from '@crcls/blocksui-sdk';

const MoonmailConnector = ({ config }) => {
  return <BlockContainer config={config} />;
};

export default MoonmailConnector;
